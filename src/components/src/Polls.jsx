import React from 'react';
import styles from './Polls.scss';

const Polls = () => {
  const x = 1;
  console.log(x);
  return (
    <div className={styles.container}>
      Currently, there is no instructor activiity
    </div>
  );
};

export default Polls;
